# Python deps for identification (Pinocchio must be installed separately via conda/pip)
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
trimesh>=4.0
pyglet>=1.5,<2

# Pinocchio + hppfcl: prefer conda-forge
#   conda install -c conda-forge pinocchio
# or follow https://stack-of-tasks.github.io/pinocchio/
